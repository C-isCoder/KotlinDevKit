package com.baichang.android.library.kotlin.config

class Configuration<T> {
  private var API_DEFAULT_HOST: String = ""

  companion object {
    fun init(builder: ConfigurationBuilder) {

    }
  }

  inner class ConfigurationBuilder {
    fun set()
  }
}